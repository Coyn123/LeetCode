def opposite_ends(arr: list) -> int:
  left = 0
  right = len(arr) - 1

  ans = 0

  while left < right:
      #Perform logic before counter change
      if CONDITION:
        left += 1
      else:
        right += 1
        
  return ans
